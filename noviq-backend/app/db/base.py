# Imported by Alembic's env.py so Base.metadata is aware of every model
# before autogenerate diffs the database.
from app.db.base_class import Base  # noqa: F401
from app.models import Analysis, Applicant, JobDescription, JobMatch, Report, RevokedRefreshToken, Resume, User  # noqa: F401
